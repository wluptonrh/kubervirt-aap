plugin: kubevirt.core.kubevirt
connections:
  - host: https://api.apps.gx54b.dynamic.redhatworkshops.io #"{{ lookup('ENV', 'K8S_AUTH_HOST') }}"
    #api_key: "{{ lookup('ENV', 'K8S_AUTH_API_KEY') }}"
    validate_certs: false
    namespaces:
      - kubevirt
